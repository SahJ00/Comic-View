//REGLAS DE ACCESO PÚBLICO (SIMPLIFICACIÓN PARA EL EJEMPLO)
{
  "rules": {
    ".read": true,
    ".write": true
  }
}

/*
{
  "rules": {
    ".read": true,
    ".write": true,
    "menu":{
      ".validate": "newData.hasChildren(['name','description', 'options','photo']) && newData.child('name').isString() && newData.child('description').isString() && newData.child('description').val().length < 500"
    }
  }
}
*/